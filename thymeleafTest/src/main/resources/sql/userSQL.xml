<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.example.spring.dao.UserDAO"> 
<!-- mybatis-confg.xml 에 Alias 되어 있습니다. --> 
	<select id="selectUserList" resultType="com.example.spring.user.UserDTO"> 
		SELECT * FROM USERS
	</select>

	<insert id="addUserInfo" parameterType="com.example.spring.user.UserDTO"> 
		INSERT INTO USERS
		VALUES(USER_SEQ.NEXTVAL, #{user_name}, #{user_id}, #{user_pw})
	</insert>

</mapper> 
